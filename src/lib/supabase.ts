/**
 * Placeholder for future Supabase client.
 * When connecting Supabase:
 * - npm install @supabase/supabase-js
 * - Set env NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY
 * - Replace this with: createBrowserClient() for client, createServerClient() for server
 */
// export const supabase = createBrowserClient(...);

export const supabase = null;
